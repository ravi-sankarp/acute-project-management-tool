import { Box, styled } from '@mui/material';

const StyledCenteredDiv = styled(Box)({
  width: '100%',
  outline: 'none',
  display: 'flex',
  alignItems: 'center',
  justifyContent: 'center'
});

export default StyledCenteredDiv;
